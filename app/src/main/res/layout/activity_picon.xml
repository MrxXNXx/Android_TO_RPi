<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    >
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="成功连接"
        android:textSize="40dp"
        android:id="@+id/succeed"
        android:layout_centerHorizontal="true"
        />
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="下"
        android:layout_alignParentBottom="true"
        android:layout_marginLeft="60dp"
        android:layout_marginBottom="30dp"
        android:id="@+id/send_down" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"

        android:layout_above="@+id/send_down"
        android:layout_alignLeft="@+id/send_down"
        android:layout_alignStart="@+id/send_down"
        android:layout_marginBottom="14dp"
        android:text="上"
        android:id="@+id/send_up"
        />

    <Button
        android:id="@+id/send_right"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/send_up"
        android:layout_toEndOf="@+id/succeed"
        android:layout_toRightOf="@+id/succeed"
        android:text="右" />

    <Button
        android:id="@+id/send_left"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/send_right"
        android:layout_alignBottom="@+id/send_right"
        android:layout_marginEnd="14dp"
        android:layout_marginRight="14dp"
        android:layout_toLeftOf="@+id/send_right"
        android:layout_toStartOf="@+id/send_right"
        android:text="左" />
</RelativeLayout>
